<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sanamchai.drivergps">

    <!-- ===== Camera / QR ticket scanner ===== -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ===== GPS & Location ===== -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Background location (Android 10+) — จำเป็นสำหรับส่ง GPS ตลอดเวลา -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ===== Wake Lock — ป้องกัน CPU หลับ ===== -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ===== Foreground Service ===== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- ===== Network ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ===== Boot (เปิดแอปอัตโนมัติหลังรีสตาร์ท) ===== -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- ===== Notification (Android 13+) ===== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ===== Battery Optimization Exemption Request ===== -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- ===== Install APK (สำหรับระบบอัพเดทแอพอัตโนมัติ) ===== -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- สิทธิ์ระดับ signature สำหรับสัญญาณ restart ภายในแอพเท่านั้น — แอพอื่นในเครื่อง
         ขอสิทธิ์นี้ไม่ได้ (ต้อง sign ด้วย key เดียวกับแอพนี้เท่านั้น) กันไม่ให้แอพอื่นสั่ง
         restart service ปลอมได้ -->
    <permission
        android:name="com.sanamchai.drivergps.PERMISSION_RESTART"
        android:protectionLevel="signature" />
    <uses-permission android:name="com.sanamchai.drivergps.PERMISSION_RESTART" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/app_cover"
        android:label="GPS Transit"
        android:roundIcon="@drawable/app_cover"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Foreground Service พร้อม foregroundServiceType=location -->
        <service
            android:name=".GpsService"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false" />

        <!-- FCM — รับสัญญาณปลุกแอพกรณีถูก Android ฆ่าโปรเซสทิ้ง -->
        <service
            android:name=".DriverFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- FileProvider — สำหรับเปิดไฟล์ APK ที่ดาวน์โหลดมาเพื่อติดตั้ง -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Boot Receiver — เปิด service อัตโนมัติหลังเปิดเครื่อง -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- Huawei/OPPO/Xiaomi boot -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.coloros.action.startservice" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Restart Receiver — รับสัญญาณ restart จากตัวแอพเองเท่านั้น ป้องกันด้วย signature
             permission แยกออกจาก BootReceiver เพราะ BootReceiver ต้อง exported เสมอสำหรับ
             BOOT_COMPLETED ซึ่งไม่มีทางเช็คสิทธิ์ผู้ส่งได้ -->
        <receiver
            android:name=".RestartReceiver"
            android:exported="true"
            android:permission="com.sanamchai.drivergps.PERMISSION_RESTART">
            <intent-filter>
                <action android:name="com.sanamchai.drivergps.RESTART" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
