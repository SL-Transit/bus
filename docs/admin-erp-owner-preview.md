# Admin ERP Owner Preview

The isolated preview is served at `/admin-erp-preview.html`. It reads only
the approved Admin data sources and keeps all write actions disabled. It does
not change `admin-erp.html`, Booking1, passenger pages, Functions, or Rules.
